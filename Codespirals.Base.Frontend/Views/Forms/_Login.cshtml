@model ILoginBase

<form>
    <div class="form-group">
        <label for="usernameOrEmail">@ComponentText.UsernameOrEmail</label>
        <input id="usernameOrEmail" class="form-control" value="@Model.UserNameOrEmail" />
    </div>

    <div class="form-group">
        <label for="password">@ComponentText.Password</label>
        <input id="password" type="password" class="form-control" value="@Model.Password" />
    </div>

    <div class="form-group form-check">
        <input type="checkbox" id="stayLoggedIn" class="form-check-input" value="@Model.StayLoggedIn"/>
        <label class="form-check-label" for="stayLoggedIn">@ComponentText.StayLoggedIn</label>
    </div>

    <button type="submit" class="btn btn-primary">@ComponentText.Login</button>
</form>