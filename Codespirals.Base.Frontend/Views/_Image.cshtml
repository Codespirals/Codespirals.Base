@model IImageBase

<div class="image-container">
    <img src="@image.Url" alt="@image.AltText" />
    @if (!string.IsNullOrWhiteSpace(image.Credit))
    {
        <small>@ComponentText.Credit: @image.Credit</small>
    }
</div>